name: marp-build
on:
  push:
    branches: [ "main" ]
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Marp Build (README)
        uses: docker://marpteam/marp-cli:v2.2.2
        with:
          args: slide-deck.md -o build/index.html
        env:
          MARP_USER: root:root
